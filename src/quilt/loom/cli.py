"""Loom CLI. Subcommands are added per phase (promote, propose-push, run, ...).

P1 ships the entry point only; no subcommands yet.
"""


def main(argv=None):
    raise SystemExit(
        "loom: no subcommands yet — see docs/plans/2026-06-06-loom-00-master.md"
    )


if __name__ == "__main__":
    main()
